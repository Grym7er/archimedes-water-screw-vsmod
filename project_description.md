# To Do:

1. Add a compatibility patch to work with the waterfall mod
2. ~~Let the mod generate the config in ModConfig~~ — tuning is via Config Lib (`ModConfig/archimedes_screw.yaml`) patching `config/settings.json`; the mod reads that asset after patches apply.
3. Increase the flow distance of archimedes water - vanilla flows 7, let's see if we can get archimedes water to flow 21.