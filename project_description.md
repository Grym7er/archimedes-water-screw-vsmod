# To Do:

1. Add a compatibility patch to work with the waterfall mod
2. Let the mod generate the config in ModConfig and use the values from that config
3. Add a brief handbook section explaining how it works